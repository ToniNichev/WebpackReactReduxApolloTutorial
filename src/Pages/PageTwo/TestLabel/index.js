import React from 'react';

const MOD_URL_BASE = 'apps.cnbc.com';

const PageTwo = (props) => {
  const {
    testOne,
    testTwo
  } = props;

  
  return(<div>
        {testOne}  
        </div>);
}
export default PageTwo;
